const login = {
  render: async () => {
    const view = `
        <section class="section">
        <img src="./images/logo.png" alt="">
        </section>
    `;
    return view;
  },
};

export default login;
